<resources>
    <!-- Base application theme. -->
    <style name="Theme.Selfie" parent="Theme.Material3.Dark">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Secondary container -->
        <item name="colorOnSecondaryContainer">@color/white</item>
        <item name="colorSecondaryContainer">#2a3232</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <!-- Customize your theme here. -->
        <item name="dialogCornerRadius">28dp</item>
    </style>

    <!-- Main activity theme. -->
    <style name="Theme.Selfie.Main" parent="Theme.Selfie">
        <item name="android:windowBackground">?attr/colorOnPrimary</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Settings activity theme. -->
    <style name="Theme.Selfie.Settings" parent="Theme.Selfie">
        <item name="android:switchStyle">@style/Theme.Selfie.Settings.Switch</item>
    </style>

    <!-- Settings switch theme. -->
    <style name="Theme.Selfie.Settings.Switch" parent="@android:style/Widget.Material.CompoundButton.Switch">
        <item name="android:switchMinWidth">52dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:track">@drawable/settingslib_switch_track</item>
        <item name="android:thumb">@drawable/settingslib_switch_thumb</item>
    </style>

    <!-- Secondary bar icons theme -->
    <style name="SelfieSecondaryBarButton">
        <item name="tint">@color/secondary_bar_button_icon</item>
        <item name="backgroundTint">?attr/colorOnPrimary</item>
        <item name="android:padding">0dp</item>
        <item name="android:textColor">@color/secondary_bar_button_icon</item>
    </style>

    <!-- Camera mode bar icons theme -->
    <style name="SelfieModeSelectorButton" parent="@style/Widget.Material3.Button.TonalButton">
        <item name="android:backgroundTint">@color/camera_mode_selector_icon</item>
        <item name="android:textColor">@color/camera_mode_selector_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">monospace</item>
    </style>

    <!-- Camera mode bar icons highlight theme -->
    <style name="SelfieModeSelectorButtonHighlight" parent="@style/SelfieModeSelectorButton">
        <item name="android:backgroundTint">@color/camera_mode_selector_icon_highlight</item>
    </style>

    <!-- Video duration button theme -->
    <style name="SelfieVideoDurationButton" parent="@style/SelfieModeSelectorButton">
        <item name="android:backgroundTint">@color/rec_red</item>
    </style>
</resources>
