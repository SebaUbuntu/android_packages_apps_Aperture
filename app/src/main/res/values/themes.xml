<resources>
    <!-- Base application theme. -->
    <style name="Theme.Selfie" parent="Theme.Material3.Dark">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Secondary container -->
        <item name="colorOnSecondaryContainer">@color/white</item>
        <item name="colorSecondaryContainer">#2a3232</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <!-- Customize your theme here. -->
        <item name="dialogCornerRadius">28dp</item>
    </style>

    <!-- Main activity theme. -->
    <style name="Theme.Selfie.Main" parent="Theme.Selfie">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Settings activity theme. -->
    <style name="Theme.Selfie.Settings" parent="Theme.Selfie" />

    <!-- Top bar icons theme -->
    <style name="SelfieTopBarButton">
        <item name="tint">?attr/colorPrimary</item>
        <item name="backgroundTint">?attr/colorOnPrimary</item>
        <item name="android:padding">0dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>

    <!-- Bottom bar icons theme -->
    <style name="SelfieModeSelectorButton" parent="@style/Widget.Material3.Button.TonalButton">
        <item name="android:backgroundTint">@color/camera_mode_selector_icon</item>
        <item name="android:textColor">@color/camera_mode_selector_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">monospace</item>
    </style>

    <!-- Shutter button theme -->
    <style name="SelfieShutterButton">
        <item name="android:background">@drawable/shutter_button_background</item>
    </style>
</resources>